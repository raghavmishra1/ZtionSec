# Render.com deployment configuration for ZtionSec Backend API
services:
  - type: web
    name: ztionsec-api
    env: python
    plan: free
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
      python startup_optimize.py
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: |
      python manage.py migrate --run-syncdb
      python startup_optimize.py
      gunicorn Ztionsec.wsgi:application --config gunicorn.conf.py
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: Ztionsec.settings_deploy
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: ztionsec-db
          property: connectionString
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "True"
      - key: ENABLE_PORT_SCANNING
        value: "False"
      - key: RATE_LIMIT_SCANS_PER_HOUR
        value: "5"
      - key: RATE_LIMIT_API_CALLS_PER_HOUR
        value: "50"
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: PYTHONDONTWRITEBYTECODE
        value: "1"
      - key: WEB_CONCURRENCY
        value: "2"
    healthCheckPath: /api/v1/health/
    
  - type: pserv
    name: ztionsec-db
    env: postgresql
    plan: free
    databaseName: ztionsec
    databaseUser: ztionsec
